Bạn cần cài đặt concurrently để có thể chạy nhiều lệnh đồng thời:

```sh
npm install -g concurrently
```

Đảm bảo rằng các script có quyền thực thi:
 ```sh
chmod +x startClientServer.sh stopClientServer.sh
 ```